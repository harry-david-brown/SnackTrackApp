# Run type-check (fast, catches type errors early)
npm run type-check

# Run lint on entire codebase (catches all issues)
npm run lint

# Run lint-staged to auto-fix staged files (complements full lint)
npx lint-staged
